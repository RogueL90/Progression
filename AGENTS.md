# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

## Temporarily disabled features

- **Push / local reminder notifications** are off (`NOTIFICATIONS_ENABLED = false`).
  To restore the exact previous behavior, follow [docs/NOTIFICATIONS.md](docs/NOTIFICATIONS.md).
