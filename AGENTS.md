# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

## Temporarily disabled features

- **Push / local reminder notifications** are off (`NOTIFICATIONS_ENABLED = false`).
  To restore: [docs/NOTIFICATIONS.md](docs/NOTIFICATIONS.md).

- **Face mesh capture** is off (`FACE_MESH_ENABLED = false`) so the app can run in **Expo Go**.
  To restore: [docs/FACE_MESH.md](docs/FACE_MESH.md).
  Day-to-day while disabled: `npx expo start` → scan with Expo Go.
